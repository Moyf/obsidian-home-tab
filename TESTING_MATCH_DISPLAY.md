# 测试 Alias 和 Title 匹配显示功能

现在插件已经扩展了匹配显示系统，支持显示 alias 和 title 匹配，就像 heading 匹配一样。

## 功能说明

当搜索结果匹配到最高优先级的 alias 或 title 时，会在结果下方显示匹配信息：

- **Alias 匹配**: 显示 `🔗 Alias of filename`
- **Title 匹配**: 显示 `# Title: matched_title` 
- **Heading 匹配**: 显示 `# matched_heading`（原有功能）

## 测试场景

### 1. Alias 匹配测试
1. 创建一个文件，添加别名：
   ```yaml
   ---
   aliases: [游戏, Games, 游戏开发]
   ---
   ```
2. 搜索 "游戏" 或 "Games"
3. **预期结果**: 
   - 主显示名称为匹配的别名
   - 下方显示 "🔗 Alias of 原文件名"

### 2. Title 匹配测试
1. 创建一个文件，设置标题：
   ```yaml
   ---
   title: "我的项目文档"
   ---
   ```
2. 搜索 "项目文档"
3. **预期结果**:
   - 主显示名称为匹配的标题
   - 下方显示 "# Title: 我的项目文档"

### 3. Heading 匹配测试（原功能验证）
1. 在文件中添加标题：
   ```markdown
   # 配置说明
   ## 安装步骤
   ```
2. 搜索 "配置" 或 "安装"
3. **预期结果**:
   - 主显示名称为文件名
   - 下方显示匹配的标题内容

### 4. 优先级测试
1. 创建一个文件，同时有：
   - 文件名包含关键词
   - Alias 包含关键词
   - Title 包含关键词
   - Heading 包含关键词
2. 搜索关键词
3. **预期结果**: 按优先级显示（文件名 > Alias > Title > Heading）

## 调试模式

启用 Debug 模式可以在控制台查看详细的匹配分析：

1. 打开插件设置
2. 启用 "Debug Mode"  
3. 打开浏览器开发者工具
4. 进行搜索，查看控制台输出

## 设置选项

确保以下设置已启用：
- "Search in titles": 启用 title 搜索
- "Search in headings": 启用 heading 搜索
- "Heading Jump Strategy": 设置为 "Smart" 以获得最佳体验